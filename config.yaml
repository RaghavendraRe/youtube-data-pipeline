# YouTube Data Collection Configuration
# This file contains non-sensitive configuration
# Sensitive data (API keys) should be in environment variables or GitHub Secrets

youtube:
  channel_id: "UCUDKhNE_Y_DVkFx1nFNZTWQ"  # Brahmakumaris
  channel_name: "Brahmakumaris"
  
  # Data collection settings
  # Data collection settings
  days_history: 36500  # 100 years (Full History)
  max_results_per_page: 50
  
  # Users Requested Fields
  fields:
    - "Video ID"
    - "Video URL"
    - "Title"
    - "Published Date"
    - "Duration (Sec)"
    - "Duration (ISO)"
    - "Is Short"
    - "Is Live"
    - "Video Type"
    - "Views"
    - "Likes"
    - "Comments"
    - "Engagement"
    - "Engagement Rate"
    - "Channel ID"
    - "Channel Title"
    - "Definition (HD/SD)"
    - "Live Start"
    - "Live End"
    - "Last Updated"

logging:
  level: "INFO"
  format: "%(asctime)s - %(levelname)s - %(message)s"
